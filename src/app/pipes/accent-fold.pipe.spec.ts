import {AccentFoldPipe} from './accent-fold.pipe';

describe('AccentFoldPipe', () => {
  it('create an instance', () => {
    const pipe = new AccentFoldPipe();
    expect(pipe).toBeTruthy();
  });
});
